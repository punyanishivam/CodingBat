public int sum2(int[] nums) {
  int sum = 0;
  if (nums.length >= 2) {
    sum = nums[0] + nums[1];
  }
  else {
    for (int i = 0; i < nums.length; i++) {
      sum += nums[i];
    }
  }
  return sum;
}
