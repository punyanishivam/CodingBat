public String repeatSeparator(String word, String sep, int count) {
  String result = "";
  for (int i = 0; i < count; i++) {
    if (i == (count - 1)) {
      result += word;
    }
    else {
      result += word + sep;
    }
  }
  return result;
}
