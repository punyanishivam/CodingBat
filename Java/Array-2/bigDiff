public int bigDiff(int[] nums) {
  int max = 0;
	for (int i = 0; i < nums.length; i++) {
		if (nums[i] > max) {
		  max = nums[i];
		}
	}
	int min = max;
	  for (int i = 0; i < nums.length; i++) {
		  if (nums[i] < min) {
		    min = nums[i];
		  }
		}
	return max - min;
}
