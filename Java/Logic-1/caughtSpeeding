public int caughtSpeeding(int speed, boolean isBirthday) {
  if(isBirthday) {
    speed -= 5;
  }
  if (speed > 60 && speed <= 80) {
    return 1;
  }
  if (speed > 80) {
    return 2;
  }
  return 0;
}
