public String missingChar(String str, int n) {
  String result = "";
  for (int i = 0; i < str.length(); i++) {
	  if(!(str.charAt(i) == str.charAt(n))) {
	    result += str.charAt(i);
	  }
  }
  return result;
}
